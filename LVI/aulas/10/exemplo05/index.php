<?php
    $a = 10;
    $b = 5;
    
    # verifica se o valor a é maior que b.
    if(10 > 5)
        echo "10 é maior 5. <br>";

    # verifica se o valor a é menor que b.
    if($b < $a) {
        echo "$b é menor que $a. <br>";
    }
    
    # verifica se o valor a é diferente b.
    if($a != $b) {
        echo "$a é diferente $b. <br>";
    }
    